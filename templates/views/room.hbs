<div>{{port}}</div>

<script src="/socket.io/socket.io.js"></script>
<script>
    var portNum = {{port}};
    var socket = io('https://myappest.herokuapp.com/room', {port: portNum});

    socket.on('getUser', (data) => {
      console.log(data);
      socket.emit('room', {msg: '111', id: socket.id});
    });
</script>